---
import { Image } from 'astro:assets';
import horizontalLogo from '../assets/images/logo@2x.png';
import mainLogo from '../assets/images/logo@2x.png';

interface Props {
	variant?: 'horizontal' | 'main';
	class?: string;
}

const { variant = 'horizontal', class: className = '' } = Astro.props;
const src = variant === 'main' ? mainLogo : horizontalLogo;
---

{
	variant === 'main' ? (
		<Image
			src={src}
			alt="Eartha's Emporium"
			class={`h-auto w-56 md:w-72 lg:w-80 ${className}`}
			width={320}
			height={320}
			loading="eager"
			decoding="async"
		/>
	) : (
		<Image
			src={src}
			alt="Eartha's Emporium"
			class={className || ' h-12 w-auto md:h-18'}
			width={363}
			height={159}
			loading="eager"
			decoding="async"
		/>
	)
}
